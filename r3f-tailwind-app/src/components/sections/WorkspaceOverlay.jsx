import React from 'react';

export default function WorkspaceOverlay() {
  return (
    <div className="overlay-inner">
      <div className="max-w-copy">
        <div className="eyebrow">04 — 3D WORKSPACE</div>
        <h2 className="headline-lg">Design it. Move it. Make it yours.</h2>
        <p className="body-text">
          Select furniture, move it across the floor, rotate and scale it.
          The room responds directly — this is an editable workspace, not a
          static rendering.
        </p>
      </div>
    </div>
  );
}